int gcd(int a, int b) {
    while (b > 0) {
        if (a < b) {
            int tmp = b;
            b = a;
            a = tmp;
        }
        a = a - b;
    }
    return a;
}

int main() {
    int c = gcd(1024 * 3, gcd(3 * 2, 10));
}